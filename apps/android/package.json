{
	"name": "@toolbox/android-tasks",
	"version": "1.1.0-beta.11",
	"description": "Android tasks app — the Toolbox plugin's Tasks feature on the phone, reading a user-chosen tasks.md.",
	"license": "MIT",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc --noEmit && vite build",
		"typecheck": "tsc --noEmit",
		"sync": "cap sync android",
		"open": "cap open android"
	},
	"dependencies": {
		"@toolbox/task-core": "1.1.0-beta.11",
		"@capacitor/core": "^7.0.0",
		"@capacitor/android": "^7.0.0",
		"@capacitor/preferences": "^7.0.0",
		"@capacitor/local-notifications": "^7.0.0"
	},
	"devDependencies": {
		"@capacitor/cli": "^7.0.0",
		"typescript": "^5.4.0",
		"vite": "^5.4.0"
	}
}
