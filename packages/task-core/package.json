{
	"name": "@toolbox/task-core",
	"version": "1.1.0-beta.11",
	"description": "Obsidian-free task logic (parse, serialize, recurrence, sort) shared by the Toolbox plugin and the Android app.",
	"license": "MIT",
	"type": "module",
	"main": "src/index.ts",
	"module": "src/index.ts",
	"types": "src/index.ts",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"typecheck": "tsc -noEmit",
		"test": "vitest run"
	}
}
